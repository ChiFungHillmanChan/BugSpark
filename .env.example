# =============================================================================
# BugSpark — Root Environment Variables
# =============================================================================
# These values are used by docker-compose.yml for local development services.
# Copy this file to .env and adjust if needed:
#   cp .env.example .env
#
# For package-specific variables, see:
#   packages/api/.env.example
#   packages/dashboard/.env.example
# =============================================================================

# -- Docker Compose (PostgreSQL) ----------------------------------------------
POSTGRES_USER=bugspark
POSTGRES_PASSWORD=bugspark_dev
POSTGRES_DB=bugspark

# -- Docker Compose (MinIO / S3) ----------------------------------------------
MINIO_ROOT_USER=bugspark
MINIO_ROOT_PASSWORD=bugspark_dev
