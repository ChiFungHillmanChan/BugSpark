# =============================================================================
# BugSpark Dashboard — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values:
#   cp .env.example .env.local
# =============================================================================

# -- API URL ------------------------------------------------------------------
# The BugSpark API base URL (including /api/v1 path).
# Local development uses the API package running on port 8000.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1

# -- S3 Hostname (images) ----------------------------------------------------
# Hostname for S3-compatible storage, used in next/image remotePatterns.
# Leave empty for local development (localhost:9000 is preconfigured).
# Production example: your-bucket.s3.amazonaws.com
NEXT_PUBLIC_S3_HOSTNAME=

# -- Sentry (error tracking) -------------------------------------------------
# Optional. Leave empty to disable Sentry integration.
NEXT_PUBLIC_SENTRY_DSN=

# -- BugSpark Widget (dogfooding) ---------------------------------------------
# Optional. Set to embed the BugSpark widget on the dashboard itself.
# Set NEXT_PUBLIC_ENABLE_BUGSPARK=false to disable the widget without removing the key.
NEXT_PUBLIC_ENABLE_BUGSPARK=true
NEXT_PUBLIC_BUGSPARK_API_KEY=
